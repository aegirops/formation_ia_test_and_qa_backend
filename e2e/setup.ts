import { Test } from '@nestjs/testing';

// Configuration globale pour les tests E2E
beforeAll(async () => {
  // Configuration qui s'applique à tous les tests E2E
});

afterAll(async () => {
  // Nettoyage après tous les tests E2E
});
